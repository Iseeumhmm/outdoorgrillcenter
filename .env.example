# A generated secret for Payload (generate with: openssl rand -hex 32)
PAYLOAD_SECRET=

# Use remote Cloudflare R2 bucket and D1 database for development
# Set to 'true' to use production R2 bucket and remote D1 database
# Set to 'false' or omit to use local simulations (.wrangler/state/)
#
# Remote mode (USE_REMOTE_R2=true):
#   - Images upload to actual Cloudflare R2 bucket
#   - Database uses remote D1 instance
#   - Requires 'remote: true' in wrangler.jsonc r2_buckets config
#   - Commands: pnpm dev, pnpm seed
#
# Local mode (USE_REMOTE_R2=false):
#   - Images stored in .wrangler/state/v3/r2/
#   - Database uses local D1 (.wrangler/state/v3/d1/)
#   - Faster, no network calls, safe for experiments
#   - Commands: pnpm dev:local, pnpm seed:local
USE_REMOTE_R2=true
